<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ renderData.title or title or metadata.title }}</title>
    <meta name="description" content="{{ renderData.description or description or metadata.description }}">
    <link rel="stylesheet" href="{{ '/css/index.css?v=2345' | url }}">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
  
    <meta property="og:title" content="{{ renderData.title or title or metadata.title }} - Ideas from Shop Ireland">
    <meta property="og:description" content="{{ renderData.description or description or metadata.description }}">
    {% if headerImage %}<meta property="og:image" content="{{ metadata.url | url }}/img/posts/{{headerImage}}">{% endif %}
    <meta property="og:url" content="{{ metadata.url | url }}{{ page.url | url }}">
    <meta name="twitter:card" content="summary_large_image">
    <link rel="shortcut icon" href="/favicon.ico">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Kalam&display=swap" rel="stylesheet">
  </head>
  <body>
    <div class="page-container">
      <header>
        <h1 class="logo">
          <a href="/">ideas</a>
          <span class="logo-by">by</span>
          <a class="shopireland-link" href="https://www.shopireland.ie">shop<strong>ireland</strong>.ie</a>
        </h1>
      </header>

      <br />

      <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>
        {{ content | safe }}
      </main>

      <footer>
        <p>All content &copy; <a href="https://ideas.shopireland.ie">Shop Ireland</a></p>
        <p>If you'd like to tell us about something cool, <a href="mailto:mail@shopireland.ie">send us an email</a>!</p>
        <p>Browse and shop <a href="https://www.shopireland.ie">Amazon Ireland</a> now over at <a href="https://www.shopireland.ie">Shop Ireland</a>.<p>
      </footer>
    </div>
  </body>
  {% if customJS %}
      {% for JSFile in customJS %}
        <script src="/js/{{ JSFile }}.js"></script>
      {% endfor %}
    {% endif %}
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-G943LSTDL1"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', 'G-G943LSTDL1');
  </script>
</html>
